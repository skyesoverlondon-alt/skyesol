# Netlify Admin App

Deploy this folder as a static site.

At runtime, set inside the UI:

- the Worker base URL
- a bootstrap admin token or use Netlify Identity login

No server build step is required.
