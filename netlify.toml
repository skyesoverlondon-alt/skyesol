[build]
  publish = "."

[functions]
  # Temporary deploy safety switch:
  # Netlify function uploads currently fail because site env vars exceed
  # the 4KB-per-function limit. Pointing to an empty directory allows
  # static deploys to succeed until env vars are trimmed in dashboard.
  directory = "netlify/functions-disabled"

# The site is plain HTML/CSS/JS. We keep build intentionally empty.
# Functions are deployed via Netlify CLI (recommended) or Git-based builds.

# PWA: service worker must never be cached
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
    Service-Worker-Allowed = "/"

[[headers]]
  for = "/manifest.json"
  [headers.values]
    Cache-Control = "public, max-age=86400"
    Content-Type = "application/manifest+json"

# SkyeDocX PWA
[[headers]]
  for = "/SkyeDocx/service-worker.js"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
    Service-Worker-Allowed = "/SkyeDocx/"

[[headers]]
  for = "/SkyeDocx/sw.js"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/SkyeDocx/manifest.webmanifest"
  [headers.values]
    Cache-Control = "no-cache"
    Content-Type = "application/manifest+json"

[[headers]]
  for = "/SkyeDocx/manifest.json"
  [headers.values]
    Cache-Control = "no-cache"
    Content-Type = "application/manifest+json"
